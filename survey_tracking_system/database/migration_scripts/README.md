This directory is intended for database migration scripts (e.g. using Alembic or plain SQL files).

For now, the primary schema is defined in `../schema.sql`. Future changes can be added here incrementally.


